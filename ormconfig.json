{
  "type": "postgres",
  "host": "localhost",
  "port": 5432,
  "name": "default",
  "username": "postgres",
  "password": "docker",
  "database": "fin_api",
  "migrations": [
    "./src/database/migrations/*.ts"
  ],
  "entities": [
    "./src/modules/**/entities/*.ts"
  ],
  "cli": {
    "migrationsDir": "./src/database/migrations"
  }
}